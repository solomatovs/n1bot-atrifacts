chainlit>=2.11,<3
dishka
httpx
openai
tomli
# Pin: chainlit 2.11+ ломает ForwardRef-резолв `CodeSettings`/`Action`
# на pydantic 2.11+ — импорт chainlit падает с "CodeSettings is not
# fully defined". Держим 2.10.x до апдейта chainlit.
pydantic>=2.10,<2.11
