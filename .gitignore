# Локальные оверрайды: коммитим только *.example, реальные — скрыты.
local/*
!local/*.example

# Экспорт образов (большие файлы — не коммитим).
images/

# Runtime-сгенерированное.
.chainlit/
.files/
import/
