# Локальные (не-коммитабельные) артефакты — шаблоны с суффиксом .example
secrets/*
!secrets/*.example
pip/pip.conf
apt/sources.list
certs/ca-chain.crt
config/config.toml
config/config.debug.toml
images/

# Runtime-сгенерированное
.chainlit/
.files/
import/
